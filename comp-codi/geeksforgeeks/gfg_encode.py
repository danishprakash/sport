def encode(arr):
    enc = ''
    for i in range(len(str(arr))):
        currcount = 0
        while
    return enc

if __name__=='__main__':
    t=int(input())
    for i in range(t):
        arr=input().strip()
        print (encode(arr))
